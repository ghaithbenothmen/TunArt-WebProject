<form method="post" action="{{ path('app_concours_delete', {'refrence': concour.refrence}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ concour.refrence) }}">
<button class="btn" style="background-color: red; color: white;">Delete</button>
</form>
